<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 text-white px-4 py-2 rounded-md mb-5 inline-block" href="/logout">Logout</a>

        </div>
        <div class="flex items-start gap-3">
            <div class="w-10 h-10 rounded-md overflow-hidden">
                <img class="w-full h-full object-cover" src="/images/<%= user.profilepic %>" alt="">
            </div>
            <h3 class="text-3xl font-bold mb-5"><span class="font-light">Hello</span>, <%= user.name %>👋</h3>
        
        </div>
        <h5 class="mb-5 mt-3">You can create a new post.</h5>

        <form action="/post" method="post">
            <textarea placeholder="What's on your mind?" class="p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" id=""></textarea>
            <input class="px-3 w-40 py-2 text-sm bg-blue-500 block rounded-md mt-2" type="submit" value="Create new post" name="" id="">
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your posts.</h3>
            <!-- Posts will be displayed here -->
            <div class="postcontainer mt-5 ">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 p-4 mb-4 rounded-md border-[1px] border-zinc-700 bg-zinc-800"> 
                        <h4 class="text-blue-400 mb-2">@<%= user.username %></h4>
                        <p class="text-sm tracking-tight"><%= post.content %></p>
                        <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
                        <div class="btns flex gap-4 mt-1 tracking-tight text-sm" >
                            <a href="/Like/<%= post.id %>" class="text-red-400">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                            </a>
                            <a href="/Edit/<%= post.id %>" class="text-gray-400">
                                <%= post.likes.indexOf(user._id) === -1 ? "Edit" : "Edit" %>
                            </a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>